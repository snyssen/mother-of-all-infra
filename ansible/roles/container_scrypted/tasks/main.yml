- name: Creating folders
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
  with_items:
    - "/mnt/storage/scrypted/nvr"
    - "{{ docker_mounts_directory }}/scrypted"

- ansible.builtin.include_role:
    name: snyssen.compose_deploy
  vars:
    stack_name: "scrypted"
