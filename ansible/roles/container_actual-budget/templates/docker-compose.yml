services:
  actual-budget:
    container_name: actual-budget
    image: docker.io/actualbudget/actual-server:26.1.0@sha256:e82b7302b0db7f8fa063159e8bddc07443bb83761798dc9ba84fe1b3df62d74f
    # environment:
    #   ACTUAL_LOGIN_METHOD: header
    networks:
      - web
    labels:
      - traefik.enable=true
      - traefik.docker.network=web
      - traefik.http.routers.actual.entryPoints=websecure
      - traefik.http.routers.actual.rule=Host(`actual.{{ main_domain }}`)
      # - traefik.http.routers.actual.middlewares=authelia@docker
      - traefik.http.routers.actual.service=actual
      - traefik.http.services.actual.loadBalancer.server.port=5006
    volumes:
      - "{{ docker_mounts_directory }}/actual-budget:/data"
    restart: unless-stopped
networks:
  web:
    external: true
