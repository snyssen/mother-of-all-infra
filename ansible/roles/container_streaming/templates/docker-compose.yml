services:
  jellyfin:
    image: ghcr.io/jellyfin/jellyfin:10.11.11@sha256:45f648c382a0c8b552582fcea40e95cb17c5d475473a891cba0eb7523fb92112
    restart: unless-stopped
    container_name: jellyfin
    networks:
      - web
      - monitoring
      - ldap
    user: "33:{{ ansible_user_gid }}"
    environment:
      - JELLYFIN_DATA_DIR=/data
      - JELLYFIN_CONFIG_DIR=/config
      - JELLYFIN_CACHE_DIR=/cache
      - JELLYFIN_LOG_DIR=/logs

      - JELLYFIN_PublishedServerUrl=https://streaming.{{ main_domain }}
      - JELLYFIN_playlists__allowDuplicates=false
    devices:
      - /dev/dri:/dev/dri # allow use of Intel QuickSync for transcoding
    group_add:
      - "109" # Adds render group
    labels:
      - traefik.enable=true
      - traefik.docker.network=web
      - traefik.http.routers.jellyfin.entryPoints=websecure
      - traefik.http.routers.jellyfin.rule=Host(`streaming.{{ main_domain }}`) && !Path(`/metrics`)
      - traefik.http.services.jellyfin.loadBalancer.server.port=8096
      # jellyfin middleware, see https://jellyfin.org/docs/general/networking/traefik2.html
      - traefik.http.routers.jellyfin.middlewares=jellyfin-mw
      - traefik.http.middlewares.jellyfin-mw.headers.customResponseHeaders.X-Robots-Tag=noindex,nofollow,nosnippet,noarchive,notranslate,noimageindex
      - traefik.http.middlewares.jellyfin-mw.headers.SSLRedirect=true
      - traefik.http.middlewares.jellyfin-mw.headers.SSLHost=streaming.{{ main_domain }}
      - traefik.http.middlewares.jellyfin-mw.headers.SSLForceHost=true
      - traefik.http.middlewares.jellyfin-mw.headers.STSSeconds=315360000
      - traefik.http.middlewares.jellyfin-mw.headers.STSIncludeSubdomains=true
      - traefik.http.middlewares.jellyfin-mw.headers.STSPreload=true
      - traefik.http.middlewares.jellyfin-mw.headers.forceSTSHeader=true
      - traefik.http.middlewares.jellyfin-mw.headers.frameDeny=true
      - traefik.http.middlewares.jellyfin-mw.headers.contentTypeNosniff=true
      - traefik.http.middlewares.jellyfin-mw.headers.browserXSSFilter=true
      - traefik.http.middlewares.jellyfin-mw.headers.customFrameOptionsValue='allow-from https://streaming.{{ main_domain }}'

      - traefik.udp.routers.jellyfin-discovery.entryPoints=jellyfin-discovery
      - traefik.udp.routers.jellyfin-discovery.service=jellyfin-discovery
      - traefik.udp.services.jellyfin-discovery.loadBalancer.server.port=7359
      # TODO: fix DLNA (should be exposed as physical device)
      # - traefik.udp.routers.dlna.entryPoints=dlna
      # - traefik.udp.routers.dlna.service=dlna
      # - traefik.udp.services.dlna.loadBalancer.server.port=1900
    volumes:
      - "{{ docker_mounts_directory }}/jellyfin/data:/data"
      - "{{ docker_mounts_directory }}/jellyfin/config:/config"
      - "{{ docker_mounts_directory }}/jellyfin/cache:/cache"
      - "{{ docker_mounts_directory }}/jellyfin/logs:/logs"
      # all below refer to media folders
      - /mnt/storage/streaming/media:/data/media

  audiobookshelf:
    image: ghcr.io/advplyr/audiobookshelf:2.35.1@sha256:1eef6716183c52abafe5405e7d6be8390248ecd59c7488c44af871757ac8fc4d
    restart: unless-stopped
    container_name: audiobookshelf
    networks:
      - web
    user: "33:{{ ansible_user_gid }}"
    labels:
      - traefik.enable=true
      - traefik.docker.network=web
      - traefik.http.routers.audiobookshelf.entryPoints=websecure
      - traefik.http.routers.audiobookshelf.rule=Host(`audiobooks.{{ main_domain }}`)
    volumes:
      - /mnt/storage/streaming/media/audiobooks:/audiobooks
      - /mnt/storage/streaming/media/audiobooks_vi:/audiobooks_vi
      - /mnt/storage/streaming/media/podcasts:/podcasts
      - /mnt/storage/streaming/media/podcasts_vi:/podcasts_vi
      - "{{ docker_mounts_directory }}/audiobookshelf/config:/config"
      - "{{ docker_mounts_directory }}/audiobookshelf/metadata:/metadata"

  torrent:
    image: lscr.io/linuxserver/transmission:4.1.3@sha256:0913501f5d05c019f5e3555a3299df1d8de420fdf882c92a8857787f9fbf999d
    container_name: torrent
    network_mode: service:vpn
    environment:
      - PUID=33
      - PGID={{ ansible_user_gid }}
      - TZ={{ iana_timezone }}
    volumes:
      - /mnt/storage/torrent:/config
      - /mnt/storage/streaming/torrent:/data/torrent
    restart: unless-stopped
    mem_limit: 712m
    depends_on:
      - vpn

  # transmission-port-update:
  #   image: ghcr.io/jgramling17/transmission-gluetun-port-update:latest@sha256:d14feda474ed90243f6c0fd170c7e89ea7933430693c3d7a6a948a08dc92183a
  #   container_name: transmission_port_update
  #   network_mode: service:vpn
  #   environment:
  #     - TRANSMISSION_RPC_PORT=9091
  #     # Left empty on purpose, we don't have auth enabled on Transmission
  #     - TRANSMISSION_RPC_USERNAME=
  #     - TRANSMISSION_RPC_PASSWORD=
  #   restart: "unless-stopped"

  usenet:
    image: lscr.io/linuxserver/sabnzbd:5.0.4@sha256:b67039e6739c2379f2eac8901248cdfcd78536ee34fe6948faea6b8ce8b4805b
    container_name: usenet
    network_mode: service:vpn
    environment:
      - PUID=33
      - PGID={{ ansible_user_gid }}
      - TZ={{ iana_timezone }}
    volumes:
      - /mnt/storage/usenet:/config
      - /mnt/storage/streaming/usenet:/data/usenet
    restart: unless-stopped
    mem_limit: 256m
    depends_on:
      - vpn

  sonarr:
    image: lscr.io/linuxserver/sonarr:4.0.19@sha256:4b025354d338999e03bf6dbdadcdde94815d39d4a5aba5de3cdc86a56d7d6c51
    container_name: sonarr
    environment:
      - PUID=33
      - PGID={{ ansible_user_gid}}
    network_mode: service:vpn
    volumes:
      - "{{ docker_mounts_directory }}/sonarr/config:/config"
      - /mnt/storage/streaming:/data
    restart: unless-stopped
    mem_limit: 1g
    depends_on:
      - vpn

  radarr:
    image: lscr.io/linuxserver/radarr:6.3.0@sha256:2b2c1c05eb3f648d2c80dfab9486147dd7bb0ad4d77fa972fc1c5de8f1da3738
    container_name: radarr
    environment:
      - PUID=33
      - PGID={{ ansible_user_gid }}
      - TZ=Europe/Brussels
    network_mode: service:vpn
    volumes:
      - "{{ docker_mounts_directory }}/radarr/config:/config"
      - /mnt/storage/streaming:/data
    restart: unless-stopped
    mem_limit: 1g
    depends_on:
      - vpn

  lidarr:
    image: lscr.io/linuxserver/lidarr:3.1.0@sha256:1caffbd1e5ad5fe3cf6f63de4aa6941e9d31625b0d7e08b60c6eeb6cf09e3a91
    container_name: lidarr
    environment:
      - PUID=33
      - PGID={{ ansible_user_gid}}
      - TZ=Europe/Brussels
    network_mode: service:vpn
    volumes:
      - "{{ docker_mounts_directory }}/lidarr/config:/config"
      - /mnt/storage/streaming:/data
    restart: unless-stopped
    mem_limit: 1g
    depends_on:
      - vpn

  prowlarr:
    image: lscr.io/linuxserver/prowlarr:2.4.0@sha256:4fd7a166c8f46dd3370a871c250ee577d6c2ae97a0dbe0e3614b5ef736205620
    container_name: prowlarr
    environment:
      - PUID=33
      - PGID={{ ansible_user_gid}}
      - TZ=Europe/Brussels
    network_mode: service:vpn
    volumes:
      - "{{ docker_mounts_directory }}/prowlarr/config:/config"
    restart: unless-stopped
    mem_limit: 1g
    depends_on:
      - vpn

  bazarr:
    image: lscr.io/linuxserver/bazarr:1.6.0@sha256:4c30dc0bb9a5d223075e7f5d12c77bd293c4b460f86d696dbe64763104c1e88c
    container_name: bazarr
    environment:
      - PUID=33
      - PGID={{ ansible_user_gid}}
      - TZ=Europe/Brussels
    network_mode: service:vpn
    volumes:
      - "{{ docker_mounts_directory }}/bazarr/config:/config"
      - /mnt/storage/streaming:/data
    restart: unless-stopped
    mem_limit: 1g
    depends_on:
      - vpn

  pinchflat:
    image: ghcr.io/kieraneglin/pinchflat:v2025.6.6@sha256:4e975edf58f0861a5cbfe8fc6aac4851ff5a02dfc3f05ffeea4982e3084a5a4a
    container_name: pinchflat
    user: "33:{{ ansible_user_gid }}"
    restart: unless-stopped
    environment:
      # Set the timezone to your local timezone
      - TZ={{ iana_timezone }}
    network_mode: service:vpn
    volumes:
      - "{{ docker_mounts_directory }}/pinchflat/config:/config"
      - /mnt/storage/streaming/media/tv:/downloads
    depends_on:
      - vpn

  lidatube:
    image: thewicklowwolf/lidatube:0.2.53@sha256:461f78684388e8992dd0d5bb5769dbcfa29ca542bdb2e39f056753a1bf9663c7
    container_name: lidatube
    environment:
      - PUID=33
      - PGID={{ ansible_user_gid}}
    volumes:
      - "{{ docker_mounts_directory }}/lidatube/config:/lidatube/config"
      - /mnt/storage/streaming/media/music:/lidatube/downloads
      - /etc/localtime:/etc/localtime:ro
    networks:
      - web
    labels:
      - traefik.enable=true
      - traefik.http.routers.lidatube.entryPoints=websecure
      - traefik.http.routers.lidatube.rule=Host(`lidatube.{{ main_domain }}`)
      - traefik.http.services.lidatube.loadBalancer.server.port=5000
      - traefik.http.routers.lidatube.middlewares=authelia@docker
    restart: unless-stopped

  lidify:
    image: thewicklowwolf/lidify:0.2.10@sha256:15a28139a6b16ff4bc5e6cc2fae413f837f8bd1971e15918c6cc7fd7fa0dcf54
    container_name: lidify
    environment:
      - PUID=33
      - PGID={{ ansible_user_gid}}
    volumes:
      - "{{ docker_mounts_directory }}/lidify/config:/lidify/config"
      - /etc/localtime:/etc/localtime:ro
    networks:
      - web
    labels:
      - traefik.enable=true
      - traefik.http.routers.lidify.entryPoints=websecure
      - traefik.http.routers.lidify.rule=Host(`lidify.{{ main_domain }}`)
      - traefik.http.services.lidify.loadBalancer.server.port=5000
      - traefik.http.routers.lidify.middlewares=authelia@docker
    restart: unless-stopped

  peertube:
    image: chocobozzz/peertube:production-bookworm@sha256:011b0ec43d921bd60a3aa01fc7995e42cf15275925f16bee6f17a6c896ca5060
    container_name: peertube
    restart: unless-stopped
    networks:
      - web
      - monitoring
      - db
      - peertube_internal
    environment:
      - PEERTUBE_DB_USERNAME={{ peertube__db_username }}
      - PEERTUBE_DB_PASSWORD={{ peertube__db_password }}
      - PEERTUBE_DB_HOSTNAME=postgres
      - PEERTUBE_DB_NAME=peertube
      - PEERTUBE_DB_SSL=false
      - PEERTUBE_REDIS_HOSTNAME=peertube_redis
      - PEERTUBE_WEBSERVER_HOSTNAME=peertube.{{ main_domain }}
      - PEERTUBE_WEBSERVER_HTTPS=true
      # Trust proxy configuration - includes localhost, loopback, and Docker bridge network
      # Adjust peertube__trust_proxy_networks variable if your Docker network uses a different subnet
      - PEERTUBE_TRUST_PROXY={{ peertube__trust_proxy_networks | default('["127.0.0.1", "loopback", "172.18.0.0/16"]') }}
      - PEERTUBE_SECRET={{ peertube__secret }}
      - PEERTUBE_SMTP_HOSTNAME={{ smtp__host }}
      - PEERTUBE_SMTP_PORT={{ smtp__port }}
      - PEERTUBE_SMTP_USERNAME={{ smtp__user }}
      - PEERTUBE_SMTP_PASSWORD={{ smtp__pass }}
      - PEERTUBE_SMTP_FROM=peertube@{{ main_domain }}
      - PEERTUBE_SMTP_TLS={{ smtp__tls | lower }}
      - PEERTUBE_SMTP_DISABLE_STARTTLS={{ smtp__ssl | lower }}
      - PEERTUBE_ADMIN_EMAIL={{ smtp__to }}
      # OIDC configuration for Authelia integration
      # TODO: remove ? This is most probably useless as I think config cazn only be done through UI
      - PEERTUBE_PLUGIN_OFFICIAL_AUTH_OPENID_CONNECT_ENABLED={{ peertube__oidc_enabled | default('true') }}
      - PEERTUBE_PLUGIN_OFFICIAL_AUTH_OPENID_CONNECT_BUTTON_LABEL={{ peertube__oidc_button_label | default('Login with Authelia') }}
      - PEERTUBE_PLUGIN_OFFICIAL_AUTH_OPENID_CONNECT_DISCOVER_URL=https://auth.{{ main_domain }}/.well-known/openid-configuration
      - PEERTUBE_PLUGIN_OFFICIAL_AUTH_OPENID_CONNECT_CLIENT_ID={{ peertube__oidc_client_id }}
      - PEERTUBE_PLUGIN_OFFICIAL_AUTH_OPENID_CONNECT_CLIENT_SECRET={{ peertube__oidc_client_secret }}
      - PEERTUBE_PLUGIN_OFFICIAL_AUTH_OPENID_CONNECT_SCOPE=openid profile email groups
      - PEERTUBE_PLUGIN_OFFICIAL_AUTH_OPENID_CONNECT_USERNAME_PROPERTY=preferred_username
      - PEERTUBE_PLUGIN_OFFICIAL_AUTH_OPENID_CONNECT_EMAIL_PROPERTY=email
      - PEERTUBE_PLUGIN_OFFICIAL_AUTH_OPENID_CONNECT_DISPLAY_NAME_PROPERTY=name
    labels:
      - traefik.enable=true
      - traefik.docker.network=web
      - traefik.http.routers.peertube.entryPoints=websecure
      - traefik.http.routers.peertube.rule=Host(`peertube.{{ main_domain }}`)
      - traefik.http.services.peertube.loadBalancer.server.port=9000
    volumes:
      - "/mnt/storage/streaming/peertube/data:/data"
      - "{{ docker_mounts_directory }}/peertube/config:/config"
    depends_on:
      - peertube_redis

  peertube_redis:
    image: redis:8-alpine@sha256:9d317178eceac8454a2284a9e6df2466b93c745529947f0cd42a0fa9609d7005
    container_name: peertube_redis
    restart: unless-stopped
    networks:
      - peertube_internal
    volumes:
      - "{{ docker_mounts_directory }}/peertube/redis:/data"

  audiomuse_redis:
    image: redis:8-alpine@sha256:9d317178eceac8454a2284a9e6df2466b93c745529947f0cd42a0fa9609d7005
    container_name: audiomuse_redis
    restart: unless-stopped
    networks:
      - audiomuse_internal
    volumes:
      - "audiomuse_redis_data:/data"

  audiomuse_web:
    image: ghcr.io/neptunehub/audiomuse-ai:2.6.2@sha256:f9c4df0ce93a7a4bcd69d5bcd15a5cc4fdb13b37a47f84773d8a24fcd0ec2a0e
    container_name: audiomuse_web
    networks:
      - web
      - db
      - audiomuse_internal
    labels:
      - traefik.enable=true
      - traefik.docker.network=web
      - traefik.http.routers.audiomuse.entryPoints=websecure
      - traefik.http.routers.audiomuse.rule=Host(`audiomuse.{{ main_domain }}`)
      - traefik.http.services.audiomuse.loadBalancer.server.port=8000
    environment:
      SERVICE_TYPE: "flask" # Tells the container to run the Flask app
      TZ: "{{ iana_timezone }}"
      # DATABASE_URL is now constructed by config.py from the following:
      POSTGRES_USER: "{{ audiomuse__db_username }}"
      POSTGRES_PASSWORD: "{{ audiomuse__db_password }}"
      POSTGRES_DB: "{{ audiomuse__db_username }}"
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432" # Internal port — always 5432 inside the Docker network
      REDIS_URL: "redis://audiomuse_redis:6379/0" # Connects to the 'audiomuse_redis' service
      TEMP_DIR: "/app/temp_audio"
    volumes:
      - audiomuse_temp_audio_flask:/app/temp_audio # Volume for temporary audio files
    depends_on:
      - audiomuse_redis
    restart: unless-stopped

  audiomuse_worker:
    image: ghcr.io/neptunehub/audiomuse-ai:2.6.2@sha256:f9c4df0ce93a7a4bcd69d5bcd15a5cc4fdb13b37a47f84773d8a24fcd0ec2a0e
    container_name: audiomuse_worker
    networks:
      - db
      - audiomuse_internal
    environment:
      SERVICE_TYPE: "worker" # Tells the container to run the RQ worker
      TZ: "{{ iana_timezone }}"
      # DATABASE_URL is now constructed by config.py from the following:
      POSTGRES_USER: "{{ audiomuse__db_username }}"
      POSTGRES_PASSWORD: "{{ audiomuse__db_password }}"
      POSTGRES_DB: "{{ audiomuse__db_username }}"
      POSTGRES_HOST: postgres
      POSTGRES_PORT: "5432" # Internal port — always 5432 inside the Docker network
      REDIS_URL: "redis://audiomuse_redis:6379/0" # Connects to the 'audiomuse_redis' service
      TEMP_DIR: "/app/temp_audio"
    volumes:
      - audiomuse_temp_audio_worker:/app/temp_audio # Volume for temporary audio files
    depends_on:
      - audiomuse_redis
    restart: unless-stopped

  vpn:
    image: qmcgaw/gluetun:v3.41.1@sha256:1a5bf4b4820a879cdf8d93d7ef0d2d963af56670c9ebff8981860b6804ebc8ab
    container_name: vpn
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    restart: unless-stopped
    networks:
      - web
    # TODO: ...I can't actually remember why I would open any port here? I'll leave them commented for now, so I can revert the change if I find out I needed them for something (+ add some explanations!)
    # ports:
    #   - 8888:8888/tcp # HTTP proxy
    #   - 8388:8388/tcp # Shadowsocks
    #   - 8388:8388/udp # Shadowsocks
    #   - 6881:6881
    #   - 6881:6881/udp
    volumes:
      - "{{ docker_mounts_directory }}/gluetun:/gluetun"
    environment:
      # See https://github.com/qdm12/gluetun/wiki
      - VPN_SERVICE_PROVIDER={{ vpn__service_provider }}
      - VPN_TYPE=wireguard
      - WIREGUARD_PRIVATE_KEY={{ vpn__wireguard_pk }}
      - PORT_FORWARD_ONLY=on
      - VPN_PORT_FORWARDING=on
      - DNS_UNBLOCK_HOSTNAMES=news.eweka.nl
      # Timezone for accurate log times
      - TZ={{ iana_timezone }}
      # Server list updater. See https://github.com/qdm12/gluetun/wiki/Updating-Servers#periodic-update
      - UPDATER_PERIOD=24h
      - HTTP_CONTROL_SERVER_AUTH_DEFAULT_ROLE='{"auth":"apikey","apikey":"{{ vpn__control_server_api_key }}"}'
    labels:
      - traefik.enable=true
      - traefik.docker.network=web
      # Torrents
      - traefik.http.routers.torrent.entryPoints=websecure
      - traefik.http.routers.torrent.rule=Host(`torrent.{{ main_domain }}`)
      - traefik.http.routers.torrent.service=torrent
      - traefik.http.services.torrent.loadBalancer.server.port=9091
      - traefik.http.routers.torrent.middlewares=authelia@docker
      # Usenet
      - traefik.http.routers.usenet.entryPoints=websecure
      - traefik.http.routers.usenet.rule=Host(`usenet.{{ main_domain }}`)
      - traefik.http.routers.usenet.service=usenet
      - traefik.http.services.usenet.loadBalancer.server.port=8080
      - traefik.http.routers.usenet.middlewares=authelia@docker
      # Pinchflat
      - traefik.http.routers.pinchflat.entryPoints=websecure
      - traefik.http.routers.pinchflat.rule=Host(`yt.{{ main_domain }}`)
      - traefik.http.routers.pinchflat.service=pinchflat
      - traefik.http.services.pinchflat.loadBalancer.server.port=8945
      - traefik.http.routers.pinchflat.middlewares=authelia@docker
      # Sonarr
      - traefik.http.routers.sonarr.entryPoints=websecure
      - traefik.http.routers.sonarr.rule=Host(`sonarr.{{ main_domain }}`)
      - traefik.http.routers.sonarr.service=sonarr
      - traefik.http.services.sonarr.loadBalancer.server.port=8989
      - traefik.http.routers.sonarr.middlewares=authelia@docker
      # Radarr
      - traefik.http.routers.radarr.entryPoints=websecure
      - traefik.http.routers.radarr.rule=Host(`radarr.{{ main_domain }}`)
      - traefik.http.routers.radarr.service=radarr
      - traefik.http.services.radarr.loadBalancer.server.port=7878
      - traefik.http.routers.radarr.middlewares=authelia@docker
      # Lidarr
      - traefik.http.routers.lidarr.entryPoints=websecure
      - traefik.http.routers.lidarr.rule=Host(`lidarr.{{ main_domain }}`)
      - traefik.http.routers.lidarr.service=lidarr
      - traefik.http.services.lidarr.loadBalancer.server.port=8686
      - traefik.http.routers.lidarr.middlewares=authelia@docker
      # Prowlarr
      - traefik.http.routers.prowlarr.entryPoints=websecure
      - traefik.http.routers.prowlarr.rule=Host(`prowlarr.{{ main_domain }}`)
      - traefik.http.routers.prowlarr.service=prowlarr
      - traefik.http.services.prowlarr.loadBalancer.server.port=9696
      - traefik.http.routers.prowlarr.middlewares=authelia@docker
      # Bazarr
      - traefik.http.routers.bazarr.entryPoints=websecure
      - traefik.http.routers.bazarr.rule=Host(`bazarr.{{ main_domain }}`)
      - traefik.http.routers.bazarr.service=bazarr
      - traefik.http.services.bazarr.loadBalancer.server.port=6767
      - traefik.http.routers.bazarr.middlewares=authelia@docker
    # FIXME: This is a workaround for the healthcheck not recovering from a failstate even when the underlying VPN connection does, thus preventing routing to underlying apps
    # healthcheck:
    #   disable: true

networks:
  web:
    external: true
  monitoring:
    external: true
  ldap:
    external: true
  db:
    external: true
  peertube_internal:
  audiomuse_internal:

volumes:
  audiomuse_redis_data:
  audiomuse_temp_audio_flask:
  audiomuse_temp_audio_worker:
