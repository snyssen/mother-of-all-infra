# PLACEHOLDER — this file must be SOPS-encrypted before use.
# After generating the VM's age key (see .sops.yaml), run:
#   sops nix/hosts/scrypted/data/secrets.yaml
# and populate at minimum:
#   tailscale/authKey: "<your-tailscale-reusable-auth-key>"
tailscale/authKey: PLACEHOLDER_REPLACE_WITH_REAL_AUTH_KEY
